/** Main nav stripe + banner offset (<MainNavigation /> tabs + chrome). */
export const TOP_CHROME_STICKY_BASE_PX = 78;

/** Breathing room between the pinned top chrome and an in-page section rail. */
export const SECTION_RAIL_STICKY_GAP_PX = 33; // rosetta space[6]
