import "./SiteThumbnail.scss";

export default function SiteThumbnail({size="medium"}) {
    return (
        <div className={`siteThumbnail siteThumbnail--${size}`}>                        
        </div>
    );
}