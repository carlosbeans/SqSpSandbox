# Disables CRA's source-map-loader for node_modules (fixes ENOENT when nested
# paths like @sqs/*/node_modules/big.js are missing after a bad install).
# Dev still uses webpack cheap-module-source-map for your app sources.
GENERATE_SOURCEMAP=false
